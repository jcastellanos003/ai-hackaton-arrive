import { test } from '@playwright/test'

test.describe('Order Confirmation Page', () => {
  test.todo('displays success state with a fake order number')
  test.todo('shows a summary of purchased items')
  test.todo('shows estimated delivery date')
  test.todo('navigates back to home when "Continue Shopping" CTA is clicked')
})
